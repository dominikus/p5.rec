{
  "name": "p5.rec",
  "description": "p5.js screen recorder plugin",
  "version": "0.0.1",
  "source": "src/index.js",
  "main": "dist/p5.rec.js",
  "author": "Dominikus Baur <do@minik.us> (https://do.minik.us)",
  "scripts": {
    "build": "microbundle --compress",
    "build:standalone": "microbundle --compress --external none",
    "dev": "microbundle watch --external none"
  },
  "devDependencies": {
    "microbundle": "^0.12.0"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.7.1",
    "@ffmpeg/ffmpeg": "^0.8.3"
  }
}
